import Navbar from "./Navbar";

const PlantsDetailscard = ({ plants }) => {
  console.log(plants);

//   const { image, plantName, price, rating, plantId,availableStock,careLevel,description,providerName,category } = plants;
  return <div>
    <Navbar/>
    {/* <div>
        <img src={image} alt="" />
    </div>

    <h1>{plantName}</h1> */}
    <p></p>
    <p></p>
  </div>;
};

export default PlantsDetailscard;
